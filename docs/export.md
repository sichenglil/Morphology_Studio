# Export

Choose URDF, MJCF, morphology JSON, or portable package, then select an explicit file or directory. Exports never overwrite the source implicitly. Portable packages copy resolved assets and rewrite resource references. USD export requires Isaac Sim and is `NOT_AVAILABLE_LOCAL` here.
