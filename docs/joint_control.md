# Joint control

Sliders update local forward kinematics in animation frames; release commits the pose. Numeric inputs accept negative and decimal values, unit conversions, Enter/blur commit, Escape cancel, and limit feedback. Pose values do not overwrite the URDF joint origin.
