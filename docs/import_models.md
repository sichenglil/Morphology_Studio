# Import models

Use **Import model** for URDF, Xacro, MJCF, or a model directory. Xacro package references require a package map when ROS discovery is unavailable. Candidate selection and low-confidence assisted decisions require confirmation. Missing resources remain visible in validation rather than being silently replaced.
