# Assembly

Import both models, open **Assembly**, choose the parent link and child root, enter XYZ/RPY, and create a fixed joint. Names are checked for collisions. Validate the combined model before export. The workflow is not tied to a specific flange, palm, robot, or joint count.
