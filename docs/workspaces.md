# Workspaces

A workspace records source references, root transforms, committed edits, poses, and interface settings. Reopening rebuilds from the read-only source and replays edits. Keep proprietary workspaces outside the repository; `workspace/` and generated outputs are ignored.
