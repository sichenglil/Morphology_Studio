# Validation

Validation reports missing resources, duplicate names, invalid limits, disconnected structure, and conversion constraints. Errors block unsafe output; warnings require review. ROS 2 and Isaac checks are `NOT_AVAILABLE_LOCAL`, not successful validations.
