# Transform editing

Select a scene object and use the Three.js gizmo or exact XYZ/RPY inspector. Assembly mode protects internal kinematic links; kinematic mode writes a non-root link transform to its parent joint origin. Dragging previews locally and commits once on release with revision conflict checks.
